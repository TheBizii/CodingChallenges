#include <iostream>

using namespace std;

int main() {
	int N, P;
	cin >> N >> P;

	// Contestant descriptions don't seem to have an effect on anything, so just print out a given P.
	cout << P << endl;

	return 0;
}
